# Server-only values. Copy this file to .env.local and never commit that file.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

# Optional: use a compatible gateway instead of the default Anthropic API.
# ANTHROPIC_BASE_URL=https://api.anthropic.com
